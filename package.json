{
  "name": "ollamacommit",
  "displayName": "Ollama Commit Message",
  "description": "Generate Git commit messages in VS Code using Ollama with Groq, Gemini, and Codex/OpenAI fallbacks.",
  "version": "0.2.1",
  "publisher": "Nice0w0",
  "icon": "media/ollama-commit-title.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Nice0w0/ollamacommit.git"
  },
  "homepage": "https://github.com/Nice0w0/ollamacommit#readme",
  "bugs": {
    "url": "https://github.com/Nice0w0/ollamacommit/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "galleryBanner": {
    "color": "#0f172a",
    "theme": "dark"
  },
  "keywords": [
    "ollama",
    "groq",
    "gemini",
    "openai",
    "codex",
    "git",
    "commit",
    "commit message",
    "source control",
    "conventional commits",
    "ai",
    "llm"
  ],
  "activationEvents": [
    "onCommand:ollamacommit.generateCommit",
    "onCommand:ollamacommit.openSettings"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ollamacommit.generateCommit",
        "title": "Ollama Commit: Generate Commit Message",
        "category": "Ollama Commit",
        "icon": {
          "light": "media/source-control-icon.png",
          "dark": "media/source-control-icon.png"
        }
      },
      {
        "command": "ollamacommit.openSettings",
        "title": "Ollama Commit: Open Settings Page",
        "category": "Ollama Commit",
        "icon": {
          "light": "media/settings-light.svg",
          "dark": "media/settings-dark.svg"
        }
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "ollamacommit.generateCommit",
          "group": "navigation@-100",
          "when": "scmProvider == git"
        }
      ]
    },
    "configuration": {
      "title": "Ollama Commit",
      "properties": {
        "ollamacommit.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:11434",
          "description": "Base URL for the Ollama server. In WSL the extension will also try Windows host fallbacks automatically."
        },
        "ollamacommit.model": {
          "type": "string",
          "default": "qwen2.5-coder:7b",
          "description": "Ollama model to use for generating commit messages."
        },
        "ollamacommit.groqApiKey": {
          "type": "string",
          "default": "",
          "description": "Groq API key used when Ollama is unavailable."
        },
        "ollamacommit.groqModel": {
          "type": "string",
          "default": "openai/gpt-oss-20b",
          "description": "Groq model used as the first fallback when Ollama is unavailable."
        },
        "ollamacommit.geminiApiKey": {
          "type": "string",
          "default": "",
          "description": "Gemini API key used when Ollama and Groq are unavailable."
        },
        "ollamacommit.geminiModel": {
          "type": "string",
          "default": "gemini-2.0-flash-lite",
          "description": "Gemini model used as the second fallback when Ollama and Groq are unavailable."
        },
        "ollamacommit.openaiModel": {
          "type": "string",
          "default": "",
          "description": "Optional model override for the last fallback through local Codex CLI. Leave empty to use Codex's configured default model."
        },
        "ollamacommit.codexPath": {
          "type": "string",
          "default": "",
          "description": "Optional absolute path to the Codex CLI binary when the extension host PATH does not include it."
        },
        "ollamacommit.systemPrompt": {
          "type": "string",
          "default": "You are an assistant that writes professional git commit messages from staged diff.\n\nWrite the commit message in this format:\n\n<type>(optional scope): <subject>\n\n- <specific change 1>\n- <specific change 2>\n- <specific change 3>\n\nInstructions:\n- Prefer Conventional Commits\n- The subject must capture the primary purpose of the commit\n- The bullet list must enumerate the concrete changes\n- Do not give only an overall summary\n- Break down the diff into distinct meaningful modifications\n- Mention each important code/config/test/doc change separately\n- Be specific about what was changed\n\nGood bullet examples:\n- add null check before accessing user profile\n- rename orderStatus to paymentStatus in checkout flow\n- update nginx config to increase client body size limit\n- remove unused retry wrapper from payment service\n- add test coverage for invalid token handling\n- change sort order to newest-first in activity endpoint\n\nBad bullet examples:\n- improve system\n- update code\n- fix issues\n- refactor project\n- make changes\n\nConstraints:\n- subject in lowercase\n- imperative mood\n- no trailing period\n- subject max 72 characters\n- use concise bullets\n- do not include file paths unless necessary for clarity\n- do not include reasoning or analysis\n- do not include markdown fences\n\nOutput:\n- Return only the final commit message",
          "markdownDescription": "System prompt sent to the active provider before the staged diff."
        },
        "ollamacommit.enableThinking": {
          "type": "boolean",
          "default": false,
          "description": "Allow supported models to use thinking/reasoning mode internally before returning the final commit message."
        },
        "ollamacommit.maxDiffChars": {
          "type": "number",
          "default": 12000,
          "minimum": 1000,
          "description": "Maximum number of staged diff characters sent to the model."
        },
        "ollamacommit.temperature": {
          "type": "number",
          "default": 0.2,
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature passed to Ollama."
        },
        "ollamacommit.copyToClipboard": {
          "type": "boolean",
          "default": false,
          "description": "Also copy the generated commit message to the clipboard."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "compile": "npm run build",
    "watch": "tsc -w -p .",
    "vscode:prepublish": "npm run build",
    "package": "vsce package --no-dependencies --no-rewrite-relative-links",
    "publish:marketplace": "vsce publish --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.2.2",
    "typescript": "^5.8.2"
  }
}
